# The ENTIRE lefthook config a product repo carries. Real hooks live in dev-config.
# Renovate bumps `ref`.
remotes:
  - git_url: https://github.com/Software-Automation-Holdings-LLC/dev-config
    ref: v1
    configs:
      - lefthook/base.yml
