/**
 * Double Metaphone test-vector fixture — the cross-language contract.
 *
 * Each entry pins the primary + alternate code (6-symbol length; see
 * `_doubleMetaphone.ts`) for a medical / drug term. The TS implementation
 * is asserted against every vector in `FuzzyMatchAlgorithm.test.ts`; the
 * Go / PHP / C# / Python ports MUST reproduce the same fixture exactly.
 * This file is the artifact the 4-language fan-out validates against —
 * keep it framework-free so it can be parsed as plain data by any port
 * (it is a const array of plain objects, no TS-only constructs beyond the
 * type annotation).
 *
 * Vectors were generated by the reference encoder and hand-reviewed for
 * the phonetically-load-bearing cases:
 *   - `tylenol` and `tylonol` both encode `TLNL` (vowel swap, identical
 *     consonant skeleton) — the canonical phonetic-recall win.
 *   - `sertraline` and `sertralin` both encode `SRTRLN` (truncated
 *     suffix, same skeleton).
 *   - `sertaline` encodes `SRTLN`, which DIFFERS from `sertraline`'s
 *     `SRTRLN`: the dropped `r` is a real consonant loss, so the phonetic
 *     codes legitimately diverge. That misspelling is recovered by the
 *     Damerau tier (one insertion), NOT the phonetic tier — exactly the
 *     tier each error class belongs to.
 */
/** One pinned phonetic vector: term → expected `{ primary, alternate }`. */
export interface DoubleMetaphoneVector {
    readonly term: string;
    readonly primary: string;
    readonly alternate: string;
}
export declare const DOUBLE_METAPHONE_VECTORS: readonly DoubleMetaphoneVector[];
//# sourceMappingURL=doubleMetaphone.vectors.d.ts.map